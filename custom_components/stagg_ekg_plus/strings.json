{
  "config": {
    "flow_title": "{name}",
    "step": {
      "bluetooth_confirm": {
        "description": "Do you want to set up {name}?"
      },
      "user": {
        "description": "Select the Fellow Stagg EKG+ kettle to set up.",
        "data": {
          "address": "Device"
        }
      }
    },
    "abort": {
      "no_devices_found": "No Fellow Stagg EKG+ kettles found. Make sure the kettle is powered and within range of a Bluetooth adapter or proxy, and not connected to another app or bridge.",
      "already_configured": "This kettle is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Fellow Stagg EKG+ options",
        "description": "Choose how Home Assistant keeps in touch with the kettle over Bluetooth.",
        "data": {
          "connection_mode": "Connection mode"
        },
        "data_description": {
          "connection_mode": "Persistent keeps one connection open at all times for live updates and instant control. On demand connects only when you control the kettle from Home Assistant, stays connected while it is on, then disconnects a few seconds after it turns off to free the Bluetooth adapter. While disconnected, entities show the last known state and Home Assistant cannot detect a physical power-on (the kettle does not broadcast its state)."
        }
      }
    }
  },
  "selector": {
    "connection_mode": {
      "options": {
        "persistent": "Persistent (always connected, live updates)",
        "on_demand": "On demand (connect when controlled) - default"
      }
    }
  },
  "entity": {
    "switch": {
      "power": {
        "name": "Power"
      }
    },
    "sensor": {
      "current_temperature": {
        "name": "Current temperature"
      },
      "target_temperature": {
        "name": "Target temperature"
      },
      "countdown": {
        "name": "Hold timer"
      },
      "rssi": {
        "name": "Signal strength"
      }
    },
    "binary_sensor": {
      "hold": {
        "name": "Holding temp"
      },
      "hold_enabled": {
        "name": "Hold enabled"
      },
      "on_base": {
        "name": "On base"
      }
    }
  }
}
